apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: shopwave-namespace
spec:
  selector:
    app: notification-service
  ports:
    - protocol: TCP
      port: 8087
      targetPort: 8087
